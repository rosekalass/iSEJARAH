# Validation report

- JavaScript syntax: PASS (`config.js`, `tailwind.config.js`, `app.js`, `modules/upgrades.js`, `responsive.js`, `pwa.js`, `sw.js`)
- Static HTML IDs: PASS, 597 unique IDs and no duplicates
- Visual browser QA: PASS at 1365 × 850 desktop and 390 × 844 phone; login artwork and complete form remain visible without horizontal overflow
- Supplied image loading: PASS, login hero (1254 × 767), sidebar wordmark (2172 × 724) and PWA icon (1254 × 1254) return successfully
- Local asset/script/style/manifest paths: PASS, 11 references resolved
- Source preservation: v65 application engine retained in `app.js`
- Frontend secrets: PASS, publishable key only; no service-role key
- Migration safety review: PASS, additive DDL; no `drop table`, `truncate`, data delete or overwrite of historical assessment rows
- Rollover design: PASS, idempotent insert using `(student_id, academic_year)` uniqueness
- PBD print layout: PASS, dedicated A4 landscape document with compact matrix styles and resolved TP values
- Executive print: PASS, dedicated printable window independent of application visibility rules
- Upgrade module spacing: PASS, responsive 20px card gaps with 14px mobile spacing
- Individual report: PASS, three-section report covering marks comparison, Headcount/ETR, class benchmark, Near Miss, PBD progression, inherited-data status, interventions and action plan
- Individual report workbook: PASS, expanded Excel rows for marks, Headcount, PBD progression and interventions
- Individual report print: PASS, A4 page breaks and table-header repetition rules added
- GPMP class/year layout: PASS, fixed-layout five-column tables fit inside each card without horizontal scrolling
- Report preview parity: PASS, preview is arranged into the same A4 report sheets used by print and PDF
- PDF pagination: PASS, one A4 PDF page is generated from each preview sheet without image slicing
- Print protection: PASS, table headers repeat and individual rows are protected from page breaks
- Preview/PDF/print visual parity: PASS, PDF and print now reuse PNG captures of the exact rendered preview sheets
- Output consistency: PASS, both output paths share one `captureReportPreviewSheets()` pipeline
- A4 print isolation: PASS, captured pages print at 210 × 297 mm with no layout reflow
- Semantic table alignment: PASS, names/classes/descriptions left-aligned while marks, TP, percentages, grades and status remain centred
- GPMP card spacing: PASS, 14px internal gap, bordered table shell and no horizontal overflow
- Filter alignment: PASS, student/class options left-aligned without changing centred PBD/marks entry controls
- CSS structure: PASS, opening and closing rule braces balanced
- Unified table shells: PASS, application tables receive responsive inner gaps, borders and rounded containment
- Horizontal fit: PASS, legacy minimum widths overridden for dashboard, marks, PBD, analytics, Headcount and comparison tables
- Theme contrast: PASS, light/dark table surfaces, headings, cells, muted text and data-entry controls use explicit contrasting colors
- Semantic alignment retained: PASS, descriptive columns left-aligned and numeric/assessment columns centred
- Table headers: PASS, centred consistently across modules with whole-word wrapping
- Word integrity: PASS, forced `overflow-wrap:anywhere` overridden on primary tables; Malay labels wrap only at valid spaces
- PBD entry matrix: PASS, horizontal scrolling restored with 1,500px minimum matrix width and readable fixed widths for pupils/SP columns
- PBD scrollbar theme: PASS, visible light/dark scrollbar track and thumb colors
- Light-mode profile hero: PASS, pupil name and hero content retain explicit high contrast on the dark gradient
- Assessment date entry: PASS, Admin can edit the active Diagnostik/UPSA/UASA date while Guru receives the synchronized read-only date
- Latest-mark source: PASS, profile, dashboard, Headcount, marks analytics, intervention metrics and reports sort by assessment exam date without using score entry/update timestamps
- Legacy date safety: PASS, existing dates remain untouched and likely `1 January` template dates are flagged for manual review
- New assessment safety: PASS, new templates start without a mock date instead of silently assuming `1 January`
- Teacher report removal: PASS, Cetak/Jana Laporan is absent from the Guru navigation and rejected by role routing; Cetak Laporan Headcount is hidden and guarded while Admin retains both functions
- Teacher interventions: PASS, navigation is enabled, reads/writes sync through the shared intervention collection, and UI plus RLS enforce assigned-class scope
- Assessment-date authorization: PASS, frontend guard and `assessments_admin_write` RLS policy restrict date changes to Admin
- Theme contrast contract: PASS, light/dark primary, muted, semantic, insight-card and action-button text use explicit contrasting color pairs
- Browser theme audit: PASS, computed styles confirm dark insight cards use light foregrounds, light insight cards use dark foregrounds, and both Export Analisis actions retain saturated surfaces with white labels
- Header action protection: PASS, the pastel header rule excludes every `.text-white` action so solid and gradient buttons cannot be replaced by a white surface
- Report theme isolation: PASS, report preview sheets remain paper-white in both modes to preserve preview/PDF/print parity
- PWA manifest: PASS, standalone start URL, scope, theme colors, Malay metadata, PNG logo and maskable SVG icon are present
- PWA registration: PASS, service worker registration is restricted to secure contexts and a visible install action is exposed only when supported (with iOS Add to Home Screen guidance)
- Browser pre-cache: PASS, localhost registration requested `sw.js` and successfully fetched every same-origin shell asset listed for offline use
- Offline cache boundary: PASS, same-origin shell/assets and approved static CDN hosts are cached; Supabase/auth/API requests and non-GET writes are explicitly excluded
- Offline data safety: PASS, no offline write queue is created; the UI announces limited offline mode and reconnects the existing Supabase Realtime subscription when connectivity returns
- Netlify PWA headers: PASS, `sw.js` is served without cache and the web manifest receives the correct content type
- Headcount student visibility: PASS, desktop/tablet table and mobile cards use explicit mutually exclusive media rules instead of the conflicting `hidden md:block` utility pair
- Headcount empty state: PASS, an empty search/filter result remains visible inside the active desktop table card and as a dedicated mobile empty card
- Headcount student table layout: PASS, BIL plus 11 existing data/action columns use fixed readable widths inside a dedicated horizontal scroll container
- Headcount name integrity: PASS, pupil names remain on one line with a wide name column and safe ellipsis for exceptionally long names
- Headcount toolbar grouping: PASS, search and performance filter are adjacent on tablet/desktop and stack cleanly on narrow phones
- Responsive device audit: PASS at 390px phone, 768px tablet and 1280px desktop; document width remains within the viewport at every tested breakpoint
- Fixed header audit: PASS, `#app-topbar` remains at viewport top after more than 900px vertical scroll and the sidebar menu button stays accessible
- Mark entry mobile layout: PASS, controls stack to full width and the 920px input table scrolls inside its card without crushing labels or names
- PBD entry mobile layout: PASS, filters/actions reflow into touch-friendly grids and the full 1,420px+ SP matrix uses a dedicated horizontal scrollbar
- Dynamic table enhancement: PASS, generated and static tables receive accessible scroll regions, semantic name/description alignment and readable minimum widths
- PWA responsive cache: PASS, `responsive.js` and all three new identity assets are pre-cached; cache version advanced to `v68-1`
- Login identity: PASS, supplied hero artwork is used without removing Admin/Guru role selection or login fields
- Sidebar identity: PASS, supplied wide iSEJARAH wordmark replaces only the previous sidebar crest/title block
- PWA icon: PASS, supplied 1254 × 1254 PNG is used by the manifest, favicon, Apple touch icon and offline screen
- Application theme v69: PASS, login identity extends to sidebar, fixed topbar, cards, forms, tables and upgrade modules
- Light theme contrast: PASS, ice-blue surfaces use dark navy typography
- Dark theme contrast: PASS, deep-navy surfaces use bright blue-white typography and cyan focus states
- Responsive theme QA: PASS at 1365 × 850 and 390 × 844 with no horizontal page overflow
- Print isolation: PASS, v69 visual overrides are scoped to `@media screen`; existing A4/PDF styling is preserved
- v70 exact palette: PASS — `#071A3D`, `#041126`, `#1677FF`, `#23D5FF`, `#13CFC9`, `#D6AA52`, `#F7F9FC`
- Visual hierarchy: PASS — blue/cyan data, limited gold assessment emphasis, cyan/teal achievement states
- Chart palette: PASS — mark, PBD, Headcount and class-comparison datasets use the iSEJARAH palette; calculations remain unchanged
- Professional styling: PASS — Lucide navigation, 18px cards, restrained shadows/glow, no emoji navigation and reduced-motion support
- v70 browser QA: PASS at 1365 × 850 desktop and 390 × 844 phone with zero horizontal page overflow and no console errors
- Headcount contrast v71: PASS — navy hero, white title, blue-grey description and readable export buttons
- No-crush tables: PASS — phone shell 350px with a readable 1120px table and horizontal scrolling; page itself has zero horizontal overflow
- Names and headings: PASS — semantic name columns remain one line; headings wrap by complete words without clipping
- Fixed header: PASS — `position: fixed`, top `0` on desktop and phone, with safe-area-aware content offset
- Adaptive forms: PASS — Markah/PBD controls use desktop, tablet and phone-specific grid arrangements
- v71 browser QA: PASS at 1365 × 850 and 390 × 844; no console errors
- Report table isolation v72: PASS — jadual dalam `#report-print-area` tidak lagi menerima lebar minimum atau scroll shell daripada sistem responsif aplikasi
- A4 column fit v72: PASS — semua tujuh lajur Perbandingan Markah dan Sasaran berada dalam sempadan helaian tanpa scrollbar atau clipping
- Preview/PDF/print parity v72: PASS — PDF dan cetakan terus menggunakan imej helaian pratonton yang sama melalui `captureReportPreviewSheets()`
- Responsive preview audit v72: PASS pada 1280 px dan 390 px; jadual kekal `table-layout: fixed`, `min-width: 0`, lajur terakhir berada dalam bingkai dan halaman tidak overflow mendatar
- Header safe-area v73: PASS — header fixed menggunakan tinggi bar + `env(safe-area-inset-top)`, semua kawalan berada dalam sempadan header dan kandungan bermula selepasnya
- Header scroll v73: PASS — selepas lebih 600px skrol pada telefon, header kekal `position: fixed` dan `top: 0`
- Mobile student cards v73: PASS — 8 lajur Data Murid menjadi kad berlabel; nama dan MyKid membalut pada ruang sah tanpa clipping
- Mobile marks entry v73: PASS — setiap murid menjadi kad borang dengan Kehadiran, Markah, Gred dan Catatan; medan kekal boleh diedit dan tiada lajur frozen
- Mobile completeness/attention v73: PASS — jadual ringkasan menjadi kad dua lajur dengan tindakan sentuh bersaiz penuh
- Mobile PBD matrix v73: PASS — matriks penuh 1,480px kekal boleh dileret, manakala BIL/Nama bertukar `position: static` supaya tidak menutup SP
- Tablet responsive v73: PASS pada 768 × 1024 — jadual mengekalkan lebar semula jadi dalam scroll shell dan halaman tidak overflow
- Desktop responsive v73: PASS pada 1365 × 850 — jadual muat apabila ruang mencukupi, header kekal di sisi sidebar dan tiada overflow halaman
- Light/dark mobile v73: PASS — kad navy menggunakan teks cerah dalam dark mode dan kad cerah menggunakan teks navy dalam light mode
- Mobile assessment date v74: PASS — kad, input tarikh dan teks bantuan kekal dalam lebar ruang Pengisian Markah pada telefon
- Native horizontal table swipe v74: PASS — scroll shell memintas gaya mampatan lama, mengekalkan lebar semula jadi jadual dan menerima leretan sentuh kiri/kanan
- Supabase → Google Sheets snapshot v75: PASS — Edge Function aktif, hanya kaedah POST diterima dan permintaan tanpa akses Google Sheet ditolak dengan 401
- Initial Google Sheet sync v75: PASS — 167 murid, 244 rekod markah sesi 2026 dan 114 rekod PBD dibaca daripada projek Supabase iSEJARAH
- Single-source data contract v75: PASS — Apps Script hanya menulis ke tab laporan sumber dan tidak menghantar perubahan Google Sheet kembali ke Supabase

SQL files must still be tested against a staging clone of the actual Supabase schema before production because PostgreSQL cannot be executed offline in this project folder.
